const navLinkPrimary = (navLinkID) => {
  const getNavLink = document.getElementById(`${navLinkID}`);
  getNavLink.classList.add("active");
};

const navLinkSecondary = (navLinkID) => {
  const getNavLink = document.getElementById(`${navLinkID}`);
  getNavLink.classList.add("active");
};
